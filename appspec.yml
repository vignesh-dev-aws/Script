version: 0.0
os: linux
files:
  - source: /app
    destination: /var/www/html
hooks:
  AfterInstall:
    - location: Scripts/install_dependencies.sh
      timeout: 300
    - location: scripts/start_server.sh
      timeout: 300
